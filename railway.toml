[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python scripts/railway_bootstrap.py && python scripts/10_start_api.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
